<cairo>

  <resources>

    <resource type="input">
      <name>input</name>
      <mrcpPort>32416</mrcpPort>
      <rtpBasePort>42050</rtpBasePort>
      <maxConnects>15</maxConnects>
      <recEngines>1</recEngines>
      <recordingDir>/temp/cairo/recordingDir</recordingDir>  <!-- TODO: change to baseRecordingDir -->
      <baseGrammarDir>/temp/cairo/baseGrammarDir</baseGrammarDir>
      <!-- uncomment the following to override sphinx config file in cairo.jar -->
      <!-- sphinxConfigURL>file:///myconfigdir/sphinx-config.xml</sphinxConfigURL -->
    </resource>

    <resource type="output">
      <name>output</name>
      <mrcpPort>32417</mrcpPort>
      <rtpBasePort>42150</rtpBasePort>
      <maxConnects>15</maxConnects>
      <basePromptDir>/temp/cairo/basePromptDir</basePromptDir>
    </resource>

  </resources>

</cairo>